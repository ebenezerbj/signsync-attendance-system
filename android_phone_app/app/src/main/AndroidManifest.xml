<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Location permissions for attendance tracking -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Camera for QR codes and photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Storage for reports and data -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Background processing -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Vibration for notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Phone state for device ID -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SignSyncAttendance"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        
        <!-- Login Activity - Main Launcher -->
        <activity
            android:name=".activity.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.SignSyncAttendance.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Change PIN Activity -->
        <activity
            android:name=".activity.ChangePinActivity"
            android:exported="false"
            android:theme="@style/Theme.SignSyncAttendance" />

        <!-- Simple Employee Portal -->
        <activity
            android:name=".activity.SimpleEmployeePortalActivity"
            android:exported="false"
            android:theme="@style/Theme.SignSyncAttendance" />
        
        <!-- Enhanced Employee Portal -->
        <activity
            android:name=".activity.EnhancedEmployeePortalActivity"
            android:exported="false"
            android:theme="@style/Theme.SignSyncAttendance" />
        
        <!-- Login Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.SignSyncAttendance.NoActionBar" />
        
        <!-- Clock In/Out Activity -->
        <activity
            android:name=".ClockInOutActivity"
            android:exported="false" />
        
        <!-- Background sync service -->
        <service
            android:name=".services.SyncService"
            android:exported="false" />

    </application>

</manifest>
